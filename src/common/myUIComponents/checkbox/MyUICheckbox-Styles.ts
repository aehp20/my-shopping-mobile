import styled from 'styled-components'
import { IonCheckbox } from '@ionic/react'

export const StyledMyUICheckbox = styled(IonCheckbox)`
  --background: var(--my-ui-checkbox-background);
  --border-color: var(--my-ui-checkbox-border-color);
  --background-checked: var(--my-ui-checkbox-background-checked);
  --border-color-checked: var(--my-ui-checkbox-border-color-checked);
`
