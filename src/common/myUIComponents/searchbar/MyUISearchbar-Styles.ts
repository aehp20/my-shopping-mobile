import styled from 'styled-components'
import { IonSearchbar } from '@ionic/react'

import './MyUISearchbar-Styles.css'

export const StyledMyUISearchbar = styled(IonSearchbar)`
  --background: var(--my-ui-searchbar-background);
  color: var(--my-ui-searchbar-color);
  padding: 0;
`
