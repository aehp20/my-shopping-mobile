import styled from 'styled-components'
import { IonCard } from '@ionic/react'

export const StyledMyUIInfo = styled(IonCard)`
  display: flex;
  justify-content: center;
  align-items: center;
  color: var(--my-ui-info-color);
  background: var(--my-ui-info-background);
  box-shadow: 0px 1px 3px 0 var(--my-ui-info-box-shadow-color);
`
