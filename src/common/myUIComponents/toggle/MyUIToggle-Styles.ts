import styled from 'styled-components'
import { IonToggle } from '@ionic/react'

export const StyledMyUIToggle = styled(IonToggle)`
  --background: var(--my-ui-toggle-background);
  --handle-background: var(--my-ui-toggle-handle-background);
  --background-checked: var(--my-ui-toggle-background-checked);
  --handle-background-checked: var(--my-ui-toggle-handle-background-checked);
`
